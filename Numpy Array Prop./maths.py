import numpy as np
arr = np.array([10, 20, 30, 40])

print(arr + 5) # Adds 5 to each element in the array
print(arr * 2) # Multiplies each element by 2
print(arr - 3) # Subtracts 3 from each element
print(arr / 2) # Divides each element by 2